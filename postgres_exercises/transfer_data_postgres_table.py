#run file with python3 ("name of file.py")
#have already installed psycopg2 (python library used to connect to postgres and manipulate data)
#using following commands : 
#import psycopg2-binary
#reminder commands with example of table 'notes"
# cur.execute('SELECT * FROM notes')
#one = cur.fetchone()
#all = cur.fetchall() 

import_csv 


conn = psycopg2.connect("host=localhost dbname=zoopla user=alyssacuignet)
cur = conn.cursor()
CREATE TABLE formula_1(

)

)